import { Host, NetServer, NetClient } from 'unet'
import { Shell, Safe, Jfy, Sfy, Loop, Delay, Now, decodeENV, env, log } from 'utils'

class Depth_measurement {

    buffer = 5 /** keep data for "buffer" seconds to calculate actions **/

    constructor() {

        //

    }

    bit_posibion = () => { }

    start_new_shot = (id: string) => { }

}